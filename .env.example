# ── Copy this file to cac-backend/.env and fill in real values ──────────────
# NEVER commit the actual .env file — it is in .gitignore

# ── Server ────────────────────────────────────────────
PORT=3000
NODE_ENV=production

# ── MongoDB Atlas ─────────────────────────────────────
# Get this from: https://cloud.mongodb.com → Connect → Drivers
MONGO_URI=mongodb+srv://<user>:<password>@cluster0.xxxxx.mongodb.net/cac_couriers?retryWrites=true&w=majority

# ── JWT ───────────────────────────────────────────────
# Generate a strong secret: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=replace_with_64_char_random_string
JWT_EXPIRES_IN=8h

# ── Admin account (auto-created on first run) ─────────
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=ChangeThisStrongPassword!

# ── Email / Nodemailer (Gmail App Password) ───────────
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_SECURE=false
MAIL_USER=your-gmail@gmail.com
MAIL_PASS=your-16-char-app-password
MAIL_FROM="CAC Couriers" <noreply@yourdomain.com>
ADMIN_NOTIFY_EMAIL=info@yourdomain.com

# ── CORS ──────────────────────────────────────────────
# Comma-separated list of frontend origins allowed to call the API.
# When frontend and backend share one Railway service, this can be left blank
# because same-origin requests have no Origin header and are always allowed.
# Only needed when your frontend is on a DIFFERENT domain:
FRONTEND_ORIGIN=https://yourdomain.com,https://www.yourdomain.com

# ── Misc ──────────────────────────────────────────────
COMPANY_NAME=CAC Couriers & Shipping Company
TRACKING_BASE_URL=https://yourdomain.com/tracking-result.html
